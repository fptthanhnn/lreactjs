import React from "react";
import ReactDOM from "react-dom/client";
import Car from "./Car.js";

//asking for error heree
const contain = document.getElementById("root");
const root = ReactDOM.createRoot(contain);
root.render(Car());
